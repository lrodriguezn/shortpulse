/** Conventional Commits config — @commitlint/config-conventional is the
 *  single source of truth for allowed commit types / scopes. */
export default {
  extends: ['@commitlint/config-conventional'],
};
