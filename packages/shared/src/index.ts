/**
 * ShortPulse shared package — Zod schemas, constants, and inferred types
 * shared by the backend and the frontend so the FE/BE contract cannot drift.
 *
 * Implementation begins in Phase 1 (packages/shared) of the SDD plan.
 */
export {};
