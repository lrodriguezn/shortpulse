# ShortPulse

> URL shortener with click analytics. One container, one database, no auth.

> **Under construction.** This repository is a greenfield project being built slice-by-slice.
> Full documentation (install, Docker, env vars, architecture, tests, Dokploy deployment) lands in the
> docs slice once the application code stabilises. See `openspec/changes/add-shortpulse-app/` for the
> design and task plan.
